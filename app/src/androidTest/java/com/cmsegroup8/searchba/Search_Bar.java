/*package com.cmsegroup8.searchba;

import android.support.design.widget.BottomNavigationView;
import android.support.test.espresso.intent.rule.IntentsTestRule;

import org.junit.Rule;
import org.junit.Test;
import java.util.regex.Pattern;
import static android.support.test.espresso.intent.Intents.intended;
import static android.support.test.espresso.intent.matcher.IntentMatchers.toPackage;
import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertTrue;

public class Search_Bar {

    @Rule
    public IntentsTestRule<Home> intentsTestRule = new IntentsTestRule<>(Home.class);

    @Test
    public void validateIntentSentToPackage() {

        //what causes an activity to start
        BottomNavigationView.clickOnView(INSERT VIEW TO GET.getView(R.id.navigation_Search));


        intended(toPackage(INSERT DESTINATION ACTIVITY"com.android.phone"));
    }


}
*/